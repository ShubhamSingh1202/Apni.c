// Take a number(n) from user & output its cube(n*n*n).
#include<stdio.h>
#include<math.h>

int main(){
    float n;
    printf("the numer is : ");
    scanf("%f",&n);

    printf("the cube is %f",pow(n,3));

}